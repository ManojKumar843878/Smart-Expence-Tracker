# Smart-Expence-Tracker
 Personal Expense Tracker built with Angular. Features: add/edit/delete expenses with amount, category, date &amp; description; simple login/signup/logout; protected Dashboard &amp; Expenses pages; auto-redirect to Dashboard on refresh (if logged in). Uses standalone components, template-driven forms, in-memory service &amp; Angular Router. 
